# CLI Module

This module handles the Command Line Interface for SemantIQ-M-Benchmarks.
It uses `typer` for command routing and `rich` for formatted output.

## Responsibilities
- Parse user arguments.
- Load configuration.
- Invoke the `pipeline` module.
- Display progress and results.
