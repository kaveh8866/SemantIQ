# Datasets Module

This module stores the data used for benchmarking.

## Responsibilities
- Provide access to static datasets (JSON, CSV, Parquet).
- Handle dataset versioning.
- Ensure data integrity.

## Privacy
Local or private datasets should be stored here but excluded from version control if necessary (via `.gitignore`).
